name: DeustoBank CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Grant execute permission for Maven Wrapper
      run: chmod +x DeustoBank/DeustoBank/mvnw

    - name: Build and analyze with Maven (JaCoCo)
      run: cd DeustoBank/DeustoBank && ./mvnw -B clean verify
      
    - name: Upload JaCoCo Coverage Report
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: jacoco-report
        path: DeustoBank/DeustoBank/target/site/jacoco/